# Copy this file to admin-api.env and provide real secrets for local testing.
# Example placeholders only; do not commit production values.
API_TOKEN_1=replace-with-admin-token
API_TOKEN_2=replace-with-readonly-token
